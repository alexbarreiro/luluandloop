/* Lulu & Loop — runtime configuration.
   Leave both values empty to run in DEMO mode (simulated checkout, local-only
   studio data). Filled in when the Supabase backend + Stripe go live. */
window.LULU_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: ''
};
